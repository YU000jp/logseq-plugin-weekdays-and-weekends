{
    "name": "logseq-plugin-weekdays-and-weekends",
    "description": "Plugin for switching journal templates for weekdays and weekends.",
    "repository": "https://github.com/YU000jp/logseq-plugin-weekdays-and-weekends.git",
    "author": "YU000jp",
    "license": "MIT",
    "logseq": {
        "id": "weekdays-and-weekends",
        "title": "Weekdays and Weekends (Templates)",
        "icon": "./icon.svg",
        "main": "./dist/index.html"
    },
    "effect": true,
    "private": true,
    "scripts": {
        "preinstall": "npx only-allow pnpm",
        "clean": "rm -r ./dist/* || true",
        "dev": "vite",
        "build": "tsc && vite build --mode=dev",
        "prod": "npm run clean && vite build"
    },
    "dependencies": {
        "@logseq/libs": "^0.0.14",
        "sweetalert2": "^11.7.3"
    },
    "devDependencies": {
        "@semantic-release/changelog": "^6.0.3",
        "@semantic-release/exec": "^6.0.3",
        "@semantic-release/git": "^10.0.1",
        "@types/node": "^18.16.3",
        "conventional-changelog-conventionalcommits": "^5.0.0",
        "cz-conventional-changelog": "^3.3.0",
        "jest": "^29.5.0",
        "npx": "^10.2.2",
        "semantic-release": "^21.0.2",
        "typescript": "^5.0.4",
        "vite": "^4.3.3",
        "vite-plugin-logseq": "^1.1.2"
    },
    "config": {
        "commitizen": {
            "path": "cz-conventional-changelog"
        }
    },
    "commitlint": {
        "extends": [
            "@commitlint/config-conventional"
        ]
    },
    "husky": {
        "hooks": {
            "commit-msg": "commitlint -E HUSKY_GIT_PARAMS"
        }
    }
}